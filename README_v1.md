Act as a senior Machine Learning Engineer and System Architect.

I want to build an AI-powered feature for a Citizen Reporting System that automatically detects infrastructure issues from uploaded images.

Project Title:
Automated Detection of Infrastructure Issues for Citizen Reporting Systems

Objective:
When a citizen uploads a photo, the system should automatically analyze the image and classify the type of infrastructure issue.

Initial Classification Scope:
- Pothole


Key Requirements:
1. Build a machine learning pipeline for image classification.
2. Suggest the best model architecture (e.g., CNN, ResNet, EfficientNet, YOLO if needed).
3. Recommend whether to use transfer learning or train from scratch.
4. Identify publicly available datasets or suggest how to collect and label custom data.
5. Include data preprocessing steps (image resizing, augmentation, normalization).
6. Define training process, validation strategy, and evaluation metrics (accuracy, precision, recall, F1-score).
7. Provide sample Python implementation using TensorFlow or PyTorch.
8. Include API design for integrating the model into a web/mobile app.
9. Suggest deployment options (cloud, edge, REST API).
10. Add a method to detect fake or irrelevant uploads (e.g., non-road images).
11. Ensure scalability for thousands of uploads daily.

Constraints:
- The system should work efficiently on limited resources (LGU environment).
- Must prioritize accuracy and speed.
- Should be able to improve over time with new data (continuous learning).

Output Format:
- System architecture diagram (textual)
- Step-by-step ML pipeline
- Model recommendation with justification
- Sample code snippets
- Deployment strategy
- Risk and limitation analysis

Goal:
Help Local Government Units (LGUs) verify citizen reports faster, reduce manual checking, and detect fake or irrelevant submissions.